# 德州扑克 - 使用说明

## 快速开始

1. 打开游戏网页
2. 输入昵称，点击「创建房间」
3. 把 6 位房间号发给朋友
4. 朋友输入房间号，点击「加入房间」
5. 房主点击「开始游戏」

## 游戏规则

标准无限注德州扑克（No-Limit Texas Hold'em）：

- 每人发 2 张底牌，公共牌最多 5 张
- 四轮下注：翻牌前 → 翻牌（3张） → 转牌（1张） → 河牌（1张）
- 用底牌和公共牌组成最佳 5 张牌型
- 牌型大小：皇家同花顺 > 同花顺 > 四条 > 葫芦 > 同花 > 顺子 > 三条 > 两对 > 一对 > 高牌

## 操作说明

| 操作 | 说明 |
|------|------|
| 弃牌 | 放弃当前手牌 |
| 过牌 | 不加注直接过（无人下注时可用） |
| 跟注 | 跟上当前最高下注 |
| 加注 | 用滑块选择金额后点击加注 |
| 全下 | 押上全部筹码 |

快捷加注按钮：1/2 底池、3/4 底池、满池、全下。

## 房主功能

房主（名字后带 ★ 标记）拥有以下权限：

- **开始游戏 / 下一手**：控制游戏节奏
- **设置盲注**：点右上角「设置」，修改大小盲注
- **设置筹码**：给任意玩家调整筹码数量（用于补码或初始设置）
- **踢人**：可从房间移除玩家

## 注意事项

### 连接相关

- **不要刷新页面**：刷新会断开连接。如果不小心刷新了，页面会自动尝试重连回房间
- **网络要稳定**：WebSocket 是长连接，网络波动可能导致短暂断线（断线后会自动重连）
- **断线不会丢失座位**：短暂断线后重连，筹码和状态都在。其他玩家会看到你显示「离线」

### 服务器相关

- **服务器进程不能断**：所有房间数据都在服务器内存中，服务器重启后所有房间会丢失
- **空房间 5 分钟自动清理**：所有玩家断线超过 5 分钟，房间会被销毁
- **建议用 systemd 管理**：防止进程意外退出，自动重启（见 DEPLOY.md）
- **单服务器单实例**：不要启动多个 server.js 进程，会端口冲突

### 游戏相关

- **最少 2 人**才能开始游戏
- **最多 9 人**一桌
- **筹码归零**的玩家会自动旁观，房主可以通过设置给其补充筹码
- **每手牌独立**：房主需要手动点「下一手」开始新一局，方便大家看结果和聊天

### 公平验证

每手牌结束后会显示「验证公平性」按钮：

1. 发牌前，服务器生成随机牌序并公布其 SHA-256 哈希值
2. 牌局结束后，公开完整牌序和盐值
3. 点击验证按钮，浏览器会重新计算哈希并比对
4. 显示 ✓ 表示牌序在发牌前就已确定，未被篡改

## 聊天

桌面端右下角有聊天输入框，输入消息按回车发送。手机端不显示聊天区域。

## 常见问题

**Q: 房间号怎么分享？**
点击页面左上角的房间号，会自动复制到剪贴板。

**Q: 可以中途加入吗？**
可以，但如果牌局正在进行中，新加入的玩家需要等当前这手牌结束后才能参与。

**Q: 筹码用完了怎么办？**
让房主在「设置」里选中你，重新设置筹码数量即可。

**Q: 服务器重启了怎么办？**
所有房间数据会丢失，需要重新创建房间。建议用 systemd 保持服务稳定运行。
